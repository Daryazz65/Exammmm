<Window x:Class="Exammmm.Практика._33_list"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Exammmm.Практика"
        mc:Ignorable="d"
        Title="_33_list" Height="450" Width="800">
    <Grid>
        <ListView Name="EventsLv">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название"
                                    DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Дата"
                                    DisplayMemberBinding="{Binding Date}"/>
                    <GridViewColumn Header="Лого"
                                    DisplayMemberBinding="{Binding Logo}"/>
                    <GridViewColumn Header="Направление"
                                    DisplayMemberBinding="{Binding DirectionId}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
